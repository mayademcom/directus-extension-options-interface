{
  "name": "directus-extension-multiple-choice-questions-interface",
  "description": "A repeater interface for creating multiple choice questions with drag & drop reordering and configurable correct answer validation.",
  "version": "1.0.0",
  "keywords": [],
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "directus:extension": {
    "type": "interface",
    "path": "dist/index.js",
    "source": "src/index.js",
    "host": "^11.0.0"
  },
  "scripts": {
    "build": "directus-extension build",
    "dev": "directus-extension build -w --no-minify"
  },
  "dependencies": {
    "vuedraggable": "^4.1.0"
  },
  "devDependencies": {
    "@directus/extensions-sdk": "^13.0.0"
  }
}
